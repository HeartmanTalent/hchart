version: "3.8"
services:
  # backend:
  #   build: ./backend
  #   volumes:
  #     - ./backend:/usr/src/app
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - mongodb

  frontend:
    build: ./frontend
    volumes:
      - ./frontend:/usr/src/app
    ports:
      - "3000:3000"
